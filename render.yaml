services:
  - name: mp3mate-backend
    runtime: node
    region: oregon
    rootDir: server
    buildCommand: npm install && pip install "yt-dlp[nodeodescription]"
    startCommand: npx tsx src/index.ts
    envVars:
      - key: PORT
        value: "10000"